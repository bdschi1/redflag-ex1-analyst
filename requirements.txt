# Convenience file: installs ALL dependencies (core + dashboard + test).
# For selective installs, use pyproject.toml extras instead:
#   pip install -e "."            # core only
#   pip install -e ".[test]"      # core + test
#   pip install -e ".[dashboard]" # core + dashboard
#   pip install -e ".[dev]"       # everything + ruff

# Core dependencies
pandas>=2.1,<3
pdfplumber>=0.10,<1
python-docx>=1.1,<2

# Dashboard (optional - only needed for streamlit run app_redteam.py)
streamlit>=1.30,<2
plotly>=5.18,<7

# Testing (optional - only needed for pytest)
pytest>=8.0,<10
pytest-cov>=5.0,<8
fpdf2>=2.7,<3
